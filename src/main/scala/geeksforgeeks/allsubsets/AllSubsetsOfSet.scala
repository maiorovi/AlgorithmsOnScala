package geeksforgeeks.allsubsets

object AllSubsetsOfSet {

  def solution(arr:Array[Int]):Int = ???

  def printBinaries(n:Int):Unit = {
    var i:Int = 0
    while ( i < n) {
      println(Integer.toBinaryString(i))
      i = i + 1
    }
  }
}
